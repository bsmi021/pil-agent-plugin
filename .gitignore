# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/

# Working evidence bundles stay local; published copies live at the
# pil-agent-plugin--v0.9.3 tag. Reference images are derived from a private
# source image; tests that need one regenerate it or skip. See tests/conftest.py.
runs/
reference/

# Local credentials must never enter a plugin package.
.env
.env.*
!.env.example
*.key
*.p12
*.pfx
credentials.json
service-account*.json
