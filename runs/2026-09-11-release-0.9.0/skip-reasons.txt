Skip reasons for the 11 skips in tests.txt, from a focused `pytest -rs` re-run
over the files that contain skip conditions (249 passed, 11 skipped).

5 x tests/test_embed.py (lines 144, 166, 188, 303, 316)
    "requires onnxruntime (uv sync --extra embedding) and PIL_AGENT_EMBED_MODEL
     pointing at an ONNX model"
    Model weights are caller-supplied and never bundled. ci.yml deliberately
    leaves PIL_AGENT_EMBED_MODEL unset, so CI skips these five as well. The
    Sep 5 evidence run downloaded the pinned MobileNet into that run only,
    which is why it reported 5 more passes and 5 fewer skips.

6 x tests/test_palette_diff.py (384, 402, 421, 452) and
    tests/test_structure_diff.py (208, 218)
    "reference image not present on this machine"
    The private historical reference; the same 6 skips the Sep 5 runlog noted.
